# Python / Django
__pycache__/
*.py[cod]
.venv/
*.sqlite3
staticfiles/

# Secrets — never commit
.env

# Node / Expo
node_modules/
.expo/
dist/
*.log

# Generated native projects (recreated by `expo prebuild` / `expo run:ios`)
mobile/ios/
mobile/android/

# macOS
.DS_Store

# Database dumps — contain real data, never commit
*.sql
